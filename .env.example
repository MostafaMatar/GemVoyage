# Local Development Configuration (with Vite)
# -------------------------------------------
VITE_SUPABASE_URL=your-project-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Production Configuration (Vercel)
# -------------------------------
# Copy these with NEXT_PUBLIC_ prefix when deploying to Vercel
# NEXT_PUBLIC_SUPABASE_URL=your-project-url
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Example Values:
# VITE_SUPABASE_URL=https://xyzproject.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIs...

# Security Configuration
# -------------------
# Debug access token for production environment
DEBUG_ACCESS_TOKEN=your-debug-token-here

# Analytics Configuration
# -------------------
GA_TRACKING_ID=your-ga-tracking-id

# Feature Flags
# -------------------
INCLUDE_DEBUG=false              # Set to true to include debug tools in production
NODE_ENV=development            # Set to 'production' for production environment

# Notes:
# 1. Never commit the actual .env file
# 2. For Vercel deployment, use NEXT_PUBLIC_ prefix
# 3. Rebuild after changing environment variables
# 4. In production:
#    - Set DEBUG_ACCESS_TOKEN to a secure random string
#    - Set INCLUDE_DEBUG=false unless debugging is needed
#    - Set NODE_ENV=production
#
# Security Notes:
# - Keep DEBUG_ACCESS_TOKEN secure and rotate regularly
# - Only enable debug tools temporarily in production
# - Monitor access logs when debug is enabled
# - Use strong, unique tokens for each environment
